format_id: SPS-Harness-Fixture-Snapshot-v2
entry: abi_alias_mayalias_overlap

c_evidence:
  - ../sources/abi_alias_unproved.c

secret:
  - {arg: 0, name: secret}

public:
  - {memory_at_arg: 3, name: public_output}

expect: violation

because:
  - the admitted p equals q case makes the public reload contain secret

sps: not-run

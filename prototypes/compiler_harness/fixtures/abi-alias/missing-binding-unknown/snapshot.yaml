format_id: SPS-Harness-Fixture-Snapshot-v2
entry: abi_alias_missing_binding

c_evidence:
  - ../sources/abi_alias_unproved.c

secret:
  - {arg: 0, name: secret}

public:
  - {memory_at_arg: 3, name: public_output}

expect: unknown

because:
  - no complete alias topology says whether p and q overlap

sps: not-run

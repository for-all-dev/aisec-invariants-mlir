entry: alloca_size_high_count
observation-model: constant-time
principals:
- observer
adversaries:
  maximal:
  - - observer
hosts:
  compute:
    visibility: secret
components:
  secret-bit:
    lifecycle: entry-input
    type: bv32
    visibility: secret
outputs:
  public-sink:
    visibility: public
releases: {}

format_id: SPS-Harness-Fixture-Snapshot-v3
entry: alloca_size_high_count
c_evidence:
- fixtures/alloca-size/high-count-unknown/alloca_size_high_count.c
secret:
- arg: 0
  name: secret_bit
public:
- observable: allocation-size
expect:
  final:
    model:
      status: Unknown
      reason: AllocaSizeNotWorldStructural
    deployment: Open
    policy: Complete
    because: the secret selects allocation count 64 or 128, so the structural-size
      premise is unavailable
    reference: candidate/expected-report.json
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - alloca_size_high_count
        operation.names:
          ordered:
          - llvm.mlir.constant
          - llvm.select
          - llvm.alloca
          - llvm.store
        operation.occurrences.llvm.mlir.constant:
          count:
            min: 2
    canonicalized-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - alloca_size_high_count
        operation.names:
          ordered:
          - llvm.select
          - llvm.alloca
    candidate-bitcode:
      kind: bytes
      digest:
        manifest: candidate/artifact.json
        field: candidate_bitcode_sha256
    scanner-diagnostic:
      kind: diagnostic
      properties:
        findings:
          count:
            eq: 1
          contains:
          - secret-dependent-allocation-size
        blockers:
          count:
            eq: 0

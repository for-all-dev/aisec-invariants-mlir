format_id: SPS-Harness-Fixture-Snapshot-v2
entry: breach_compressed_length_bad

c_evidence:
  - ../sources/breach_compressed_length_bad.c

secret:
  - {arg: 0, name: secret_byte}

public:
  - {memory_at_arg: 3, name: public_wire_length}

expect: violation

because:
  - matching the public guess changes stored wire length from 32 to 31

sps: not-run

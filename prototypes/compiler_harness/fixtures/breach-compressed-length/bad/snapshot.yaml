format_id: SPS-Harness-Fixture-Snapshot-v3
entry: breach_compressed_length_bad
c_evidence:
- fixtures/breach-compressed-length/bad/breach_compressed_length_bad.c
secret:
- arg: 0
  name: secret_byte
public:
- memory_at_arg: 3
  name: public_wire_length
expect:
  final:
    model:
      status: Counterexample
      bad_state: public-wire-length-mismatch
    deployment: Open
    policy: Complete
    events:
    - kind: Output
      field: valueBytes
      id: public-wire-length
      first_bad: true
    because: matching the public guess changes stored wire length from 32 to 31
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - breach_compressed_length_bad
        operation.names:
          ordered:
          - llvm.icmp
          - llvm.zext
          - llvm.sub
          - llvm.store

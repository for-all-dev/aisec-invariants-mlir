format_id: SPS-Harness-Fixture-Snapshot-v3
entry: ckks_unsafe_release_fixed
c_evidence:
- fixtures/ckks-release/fixed/ckks_unsafe_release_fixed.c
secret:
- arg: 0
  name: raw_approximate_plaintext
public:
- memory_at_arg: 3
  name: public_release
allowed:
- public_release may reveal only a sanitizer-approved value with a valid certificate
expect:
  final:
    model:
      status: Proved
    deployment: Open
    policy: Complete
    events:
    - kind: Release
      field: valueBytes
    because: the public sink receives the sanitizer result instead of raw plaintext
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          contains:
          - ReleaseCarrierMismatch
          count:
            eq: 1
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - ckks_unsafe_release_fixed
        operation.names:
          ordered:
          - llvm.call
          - llvm.call
          - llvm.store
          - llvm.return
        call.callees:
          contains:
          - ckks_sanitize_model
          - llvm.sps.release

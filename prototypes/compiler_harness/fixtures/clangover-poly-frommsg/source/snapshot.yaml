format_id: SPS-Harness-Fixture-Snapshot-v3
entry: clangover_poly_frommsg_source
c_evidence:
- fixtures/clangover-poly-frommsg/source/clangover_poly_frommsg_vulnerable.c
secret:
- arg: 0
  name: bit
public:
- observable: timing
expect:
  final:
    model:
      status: Proved
    deployment: Open
    policy: Complete
    events:
    - kind: Latency
      field: configuredClass
    because: the branchless source shape is proved in the modeled timing semantics;
      backend behavior remains deployment evidence
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - clangover_poly_frommsg_source
        operation.names:
          ordered:
          - llvm.mlir.constant
          - llvm.sub
          - llvm.and
          - llvm.return
          excludes:
          - llvm.cond_br
        operation.occurrences.llvm.mlir.constant:
          count:
            min: 2

entry: dynamic_kv_length_bad
observation-model: constant-time
principals:
- observer
adversaries:
  maximal:
  - - observer
hosts:
  compute:
    visibility: secret
components:
  secret-length:
    lifecycle: entry-input
    type: bv32
    visibility: secret
  private-result:
    lifecycle: entry-input
    type: bv32
    visibility: secret
outputs:
  public-allocation-count:
    visibility: public
  public-iteration-count:
    visibility: public
  return:
    visibility: secret
releases: {}

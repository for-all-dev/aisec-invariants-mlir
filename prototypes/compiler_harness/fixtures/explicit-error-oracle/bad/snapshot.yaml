format_id: SPS-Harness-Fixture-Snapshot-v3
entry: explicit_error_oracle_bad
c_evidence:
- fixtures/explicit-error-oracle/bad/explicit_error_oracle_bad.c
secret:
- arg: 0
  name: padding_is_valid
- arg: 1
  name: padding_error_detail
public:
- memory_at_arg: 3
  name: public_status
- memory_at_arg: 4
  name: public_error_detail
allowed:
- public_status may reveal only padding validity
expect:
  final:
    model:
      status: Counterexample
      bad_state: public-error-detail-mismatch
    deployment: Open
    policy: Complete
    events:
    - kind: Release
      field: valueBytes
      id: padding-status
    - kind: Output
      field: valueBytes
      id: public-error-detail
      first_bad: true
    because: public_error_detail reveals more than the allowed validity bit
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          contains:
          - ReleaseCarrierMismatch
          count:
            eq: 1
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - explicit_error_oracle_bad
        operation.names:
          ordered:
          - llvm.mlir.constant
          - llvm.and
          - llvm.xor
          - llvm.call
          - llvm.store
        call.callees:
          contains:
          - llvm.sps.release
        operation.occurrences.llvm.store:
          count:
            min: 2

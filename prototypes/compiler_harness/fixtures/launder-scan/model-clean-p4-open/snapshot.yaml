format_id: SPS-Harness-Fixture-Snapshot-v3
entry: launder_scan_model_proved
c_evidence:
- fixtures/launder-scan/model-clean-p4-open/launder_scan_bad.c
secret:
- arg: 0
  name: secret
public:
- observable: control
- observable: address
- observable: timing
expect:
  final:
    model:
      status: Proved
    deployment: Open
    policy: Complete
    events:
    - kind: Memory
      field: offsetClass
    - kind: Latency
      field: configuredClass
    because: a branchless LLVM select may become a secret branch and conditional load
      on x86
    reference: candidate/expected-report.json
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - launder_scan_model_proved
        operation.names:
          ordered:
          - llvm.load
          - llvm.icmp
          - llvm.select
          - llvm.store
          excludes:
          - llvm.cond_br
    canonicalized-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - launder_scan_model_proved
        operation.names:
          ordered:
          - llvm.select
          excludes:
          - llvm.cond_br
    candidate-bitcode:
      kind: bytes
      digest:
        manifest: candidate/artifact.json
        field: candidate_bitcode_sha256
    x86-backend-risk:
      kind: assembly
      function: launder_scan_bad
      properties:
        instruction.mnemonics:
          ordered:
          - testl
          - je

format_id: SPS-Harness-Fixture-Snapshot-v3
entry: leftoverlocals_scratch_bad
c_evidence:
- fixtures/leftoverlocals-scratch/bad/leftoverlocals_scratch_bad.c
secret:
- arg: 0
  name: prior_tenant_secret
public:
- memory_at_arg: 3
  name: next_tenant_output
expect:
  final:
    model:
      status: Counterexample
      bad_state: next-tenant-output-mismatch
    deployment: Open
    policy: Complete
    events:
    - kind: Output
      field: valueBytes
      first_bad: true
    because: the prior tenant's scratch value is reloaded and published to the next
      tenant
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - leftoverlocals_scratch_bad
        operation.names:
          ordered:
          - llvm.store
          - llvm.load
        operation.occurrences.llvm.store:
          count:
            min: 2

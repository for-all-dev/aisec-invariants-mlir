format_id: SPS-Harness-Fixture-Snapshot-v2
entry: leftoverlocals_scratch_bad

c_evidence:
  - ../sources/leftoverlocals_scratch_bad.c

secret:
  - {arg: 0, name: prior_tenant_secret}

public:
  - {memory_at_arg: 3, name: next_tenant_output}

expect: violation

because:
  - the prior tenant's scratch value is reloaded and published to the next tenant

sps: not-run

source: pointer_rebinding_disjoint_select_bad.c
relation: provenance-only
entry:
  id: pointer_rebinding_disjoint_select_bad
  symbol: pointer_rebinding_disjoint_select_bad
  host: compute
  function-type: void (i32, ptr, ptr, ptr)
  return: void
carriers:
  secret-selector:
    argument: 0
    llvm-type: i32
    bit-width: 32
roots:
  left:
    argument: 1
    host: compute
    extent-bytes: 1
    alignment: 1
    permission: read-only
    initialization: initialized
    ownership: caller
    input: left-input
  right:
    argument: 2
    host: compute
    extent-bytes: 1
    alignment: 1
    permission: read-only
    initialization: initialized
    ownership: caller
    input: right-input
  private-result:
    argument: 3
    host: compute
    extent-bytes: 1
    alignment: 1
    permission: write-only
    initialization: uninitialized
    ownership: caller
    output: private-result
aliases:
  complete: true
  relations:
  - relation: disjoint
    roots:
    - left
    - private-result
  - relation: disjoint
    roots:
    - left
    - right
  - relation: disjoint
    roots:
    - private-result
    - right
terminal-output-order:
  normal-void:
  - private-result

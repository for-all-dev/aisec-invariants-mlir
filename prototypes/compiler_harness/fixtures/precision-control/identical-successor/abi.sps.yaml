source: identical_successor_control.c
relation: provenance-only
entry:
  id: identical_successor_control
  symbol: identical_successor_control
  host: compute
  function-type: i32 (i32, i32)
  return:
    llvm-type: i32
    bit-width: 32
    output: return
carriers:
  high-condition:
    argument: 0
    llvm-type: i32
    bit-width: 32
  public-value:
    argument: 1
    llvm-type: i32
    bit-width: 32
roots: {}
aliases:
  complete: true
  relations: []
terminal-output-order:
  normal-value:
  - return

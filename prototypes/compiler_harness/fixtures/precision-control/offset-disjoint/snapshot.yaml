format_id: SPS-Harness-Fixture-Snapshot-v3
entry: offset_disjoint_control
c_evidence:
- fixtures/precision-control/offset-disjoint/offset_disjoint_control.c
secret:
- arg: 0
  name: secret_byte
public:
- memory_at_arg: 3
  name: public_sink
expect:
  final:
    model:
      status: Proved
    deployment: Open
    policy: Complete
    events:
    - kind: Output
      field: valueBytes
    because: a secret store at byte offset 4 cannot affect the reload at byte offset
      8
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - offset_disjoint_control
        operation.names:
          ordered:
          - llvm.mlir.constant
          - llvm.getelementptr
          - llvm.store
          - llvm.load
        operation.occurrences.llvm.mlir.constant:
          count:
            min: 2
        operation.occurrences.llvm.getelementptr:
          count:
            min: 2
        operation.occurrences.llvm.store:
          count:
            min: 3
    canonicalized-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - offset_disjoint_control
        operation.names:
          ordered:
          - llvm.getelementptr
          - llvm.store
        operation.occurrences.llvm.getelementptr:
          count:
            min: 2
        operation.occurrences.llvm.store:
          count:
            min: 2

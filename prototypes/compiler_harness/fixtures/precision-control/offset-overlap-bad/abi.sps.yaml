source: offset_overlap_bad.c
relation: provenance-only
entry:
  id: offset_overlap_bad
  symbol: offset_overlap_bad
  host: compute
  function-type: i32 (ptr, i32, i32)
  return:
    llvm-type: i32
    bit-width: 32
    output: return
carriers:
  secret-byte:
    argument: 1
    llvm-type: i32
    bit-width: 32
  public-value:
    argument: 2
    llvm-type: i32
    bit-width: 32
roots:
  buffer:
    argument: 0
    host: compute
    extent-bytes: 9
    alignment: 1
    permission: read-write
    initialization: initialized
    ownership: caller
    input: buffer-input
    output: buffer
aliases:
  complete: true
  relations: []
terminal-output-order:
  normal-value:
  - buffer
  - return

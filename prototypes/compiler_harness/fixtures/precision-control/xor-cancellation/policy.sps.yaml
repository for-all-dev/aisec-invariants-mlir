entry: xor_cancellation_control
observation-model: constant-time
principals:
- observer
adversaries:
  maximal:
  - - observer
hosts:
  compute:
    visibility: secret
components:
  secret:
    lifecycle: entry-input
    type: bv32
    visibility: secret
outputs:
  return:
    visibility: public
releases: {}

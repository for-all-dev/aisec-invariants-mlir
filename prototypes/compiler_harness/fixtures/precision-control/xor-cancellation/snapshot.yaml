format_id: SPS-Harness-Fixture-Snapshot-v3
entry: xor_cancellation_control
c_evidence:
- fixtures/precision-control/xor-cancellation/xor_cancellation_control.c
secret:
- arg: 0
  name: secret
public:
- memory_at_arg: 1
  name: public_sink
expect:
  final:
    model:
      status: Proved
    deployment: Open
    policy: Complete
    events:
    - kind: Output
      field: valueBytes
    because: secret xor secret is zero in every run
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - xor_cancellation_control
        operation.names:
          ordered:
          - llvm.xor
          - llvm.store
    canonicalized-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - xor_cancellation_control
        operation.names:
          ordered:
          - llvm.xor

source: predecessor_choice_blockarg_bad.c
relation: provenance-only
entry:
  id: predecessor_choice_blockarg_bad
  symbol: predecessor_choice_blockarg_bad
  host: compute
  function-type: i32 (i32)
  return:
    llvm-type: i32
    bit-width: 32
    output: return
carriers:
  secret-bit:
    argument: 0
    llvm-type: i32
    bit-width: 32
roots: {}
aliases:
  complete: true
  relations: []
terminal-output-order:
  normal-value:
  - return

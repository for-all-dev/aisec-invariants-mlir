entry: predecessor_choice_blockarg_bad
observation-model: constant-time
principals:
- observer
adversaries:
  maximal:
  - - observer
hosts:
  compute:
    visibility: secret
components:
  secret-bit:
    lifecycle: entry-input
    type: bv32
    visibility: secret
outputs:
  return:
    visibility: public
releases: {}

format_id: SPS-Harness-Fixture-Snapshot-v2
entry: predecessor_choice_blockarg_bad

c_evidence:
  - ../sources/predecessor_choice_blockarg_bad.c

secret:
  - {arg: 0, name: secret_bit}

public:
  - {memory_at_arg: 1, name: public_sink}

expect: violation

because:
  - the secret predecessor choice selects block argument 10 or 20

sps: not-run

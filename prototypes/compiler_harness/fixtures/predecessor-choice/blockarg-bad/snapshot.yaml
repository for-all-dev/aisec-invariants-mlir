format_id: SPS-Harness-Fixture-Snapshot-v3
entry: predecessor_choice_blockarg_bad
c_evidence:
- fixtures/predecessor-choice/blockarg-bad/predecessor_choice_blockarg_bad.c
secret:
- arg: 0
  name: secret_bit
public:
- observable: return
expect:
  final:
    model:
      status: Counterexample
      bad_state: public-output-block-argument-mismatch
    deployment: Open
    policy: Complete
    events:
    - kind: Output
      field: valueBytes
      id: return
      first_bad: true
    because: the secret predecessor choice selects block argument 10 or 20
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - predecessor_choice_blockarg_bad
        operation.names:
          ordered:
          - llvm.mlir.constant
          - llvm.icmp
          - llvm.cond_br
        operation.occurrences.llvm.mlir.constant:
          count:
            min: 2
    canonicalized-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - predecessor_choice_blockarg_bad
        operation.names:
          ordered:
          - llvm.cond_br
    scanner-diagnostic:
      kind: diagnostic
      properties:
        findings:
          count:
            eq: 2
          contains:
          - secret-dependent-branch
          - secret-to-public-sink
        blockers:
          count:
            eq: 0

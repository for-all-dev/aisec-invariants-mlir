format_id: SPS-Harness-Fixture-Snapshot-v2
entry: prefix_causal_release_bad

c_evidence:
  - ../sources/prefix_causal_release_bad.c

secret:
  - {arg: 0, name: secret}

public:
  - {memory_at_arg: 1, name: public_channel}

allowed:
  - the release may authorize secret only from its own occurrence onward

expect: violation

because:
  - the public observation occurs before the authorized release

sps: not-run

format_id: SPS-Harness-Fixture-Snapshot-v3
entry: redis_pool_reuse_bad
c_evidence:
- fixtures/redis-pool-reuse/bad/redis_pool_reuse_bad.c
secret:
- arg: 0
  name: response_owned_by_a
public:
- observable: return
allowed:
- the return to actor B may contain only actor B's response
expect:
  final:
    model:
      status: Counterexample
      bad_state: actor-b-return-payload-mismatch
    deployment: Open
    policy: Complete
    events:
    - kind: Output
      field: valueBytes
      id: return
      first_bad: true
    because: the cancellation path returns actor A's response to actor B
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - redis_pool_reuse_bad
        operation.names:
          ordered:
          - llvm.mlir.constant
          - llvm.and
          - llvm.icmp
          - llvm.cond_br
          - llvm.return
        operation.occurrences.llvm.mlir.constant:
          count:
            min: 2
        operation.occurrences.llvm.return:
          count:
            min: 2

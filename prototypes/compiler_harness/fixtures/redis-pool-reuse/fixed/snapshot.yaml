format_id: SPS-Harness-Fixture-Snapshot-v2
entry: redis_pool_reuse_fixed

c_evidence:
  - ../sources/redis_pool_reuse_fixed.c

secret:
  - {arg: 0, name: response_owned_by_a}

public:
  - {observable: return}

allowed:
  - the return to actor B may contain only actor B's response

expect: no-violation

because:
  - the function always returns actor B's own response

sps: not-run

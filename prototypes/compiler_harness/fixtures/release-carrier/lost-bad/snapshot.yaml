format_id: SPS-Harness-Fixture-Snapshot-v3
entry: release_carrier_lost_bad
c_evidence:
- fixtures/release-carrier/lost-bad/release_carrier.c
secret:
- arg: 0
  name: raw
public:
- memory_at_arg: 3
  name: sink
- observable: release-identity
allowed:
- sink may receive only a value authorized by a bound and conforming p_invalid_callable
  release carrier
expect:
  final:
    model:
      status: Unknown
      reason: ReleaseCarrierMismatch
    deployment: Open
    policy: Complete
    because: the direct release wrapper call was lost, so policy cannot be bound to
      either marked store
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          contains:
          - ReleaseCarrierMismatch
          count:
            eq: 1
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - release_carrier_lost_bad
        operation.names:
          ordered:
          - llvm.and
          - llvm.store
          excludes:
          - llvm.call
        call.callees:
          excludes:
          - sps_release_invalid_callable
        operation.occurrences.llvm.and:
          count:
            min: 2
        operation.occurrences.llvm.store:
          count:
            min: 2

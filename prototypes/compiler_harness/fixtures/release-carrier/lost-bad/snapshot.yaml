format_id: SPS-Harness-Fixture-Snapshot-v2
entry: release_carrier_lost_bad

c_evidence:
  - ../sources/release_carrier.c

secret:
  - {arg: 0, name: raw}

public:
  - {memory_at_arg: 3, name: sink}
  - {observable: release-identity}

allowed:
  - sink may receive only a value authorized by a bound and conforming p_invalid_callable release carrier

expect: unknown

because:
  - the direct release wrapper call was lost, so policy cannot be bound to either marked store

sps: not-run

format_id: SPS-Harness-Fixture-Snapshot-v3
entry: release_carrier_marker_only_bad
c_evidence:
- fixtures/release-carrier/marker-only-bad/release_carrier.c
secret:
- arg: 0
  name: raw
public:
- memory_at_arg: 2
  name: sink
- observable: release-identity
allowed:
- sink may receive only a value authorized by a bound and conforming p_invalid_callable
  release carrier
expect:
  final:
    model:
      status: Unknown
      reason: ReleaseCarrierMismatch
    deployment: Open
    policy: Complete
    because: raw reaches the sink, but the store marker cannot be bound as a release
      carrier
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          contains:
          - ReleaseCarrierMismatch
          count:
            eq: 1
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - release_carrier_marker_only_bad
        operation.names:
          ordered:
          - llvm.store
          excludes:
          - llvm.call
          - llvm.and

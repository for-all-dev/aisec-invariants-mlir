source: secret_embedding_index_bad.c
relation: provenance-only
entry:
  id: secret_embedding_index_bad
  symbol: secret_embedding_index_bad
  host: compute
  function-type: i32 (ptr, i32)
  return:
    llvm-type: i32
    bit-width: 32
    output: return
carriers:
  secret-index:
    argument: 1
    llvm-type: i32
    bit-width: 32
roots:
  table:
    argument: 0
    host: compute
    extent-bytes: 64
    alignment: 4
    permission: read-only
    initialization: initialized
    ownership: caller
    input: table-input
aliases:
  complete: true
  relations: []
terminal-output-order:
  normal-value:
  - return

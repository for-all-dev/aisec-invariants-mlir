format_id: SPS-Harness-Fixture-Snapshot-v2
entry: secret_embedding_index_bad

c_evidence:
  - ../sources/secret_embedding_index_bad.c

secret:
  - {arg: 1, name: secret_index}

public:
  - {observable: address}

expect: violation

because:
  - the secret index chooses the table load address

sps: not-run

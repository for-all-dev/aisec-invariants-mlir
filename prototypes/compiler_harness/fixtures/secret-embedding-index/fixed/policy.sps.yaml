entry: secret_embedding_index_fixed
observation-model: constant-time
principals:
- observer
adversaries:
  maximal:
  - - observer
hosts:
  compute:
    visibility: secret
components:
  table-input:
    lifecycle: entry-input
    type: bytes
    visibility: public
  secret-index:
    lifecycle: entry-input
    type: bv32
    visibility: secret
outputs:
  return:
    visibility: secret
releases: {}

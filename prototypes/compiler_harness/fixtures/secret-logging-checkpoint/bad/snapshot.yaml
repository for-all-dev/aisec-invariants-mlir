format_id: SPS-Harness-Fixture-Snapshot-v2
entry: secret_logging_checkpoint_bad

c_evidence:
  - ../sources/secret_logging_checkpoint_bad.c

secret:
  - {arg: 0, name: service_account_token}

public:
  - {memory_at_arg: 2, name: public_log}
  - {memory_at_arg: 3, name: public_checkpoint}

expect: violation

because:
  - the token is copied directly into both public outputs

sps: not-run

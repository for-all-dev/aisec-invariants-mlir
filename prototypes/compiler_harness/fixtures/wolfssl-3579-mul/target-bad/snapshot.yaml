format_id: SPS-Harness-Fixture-Snapshot-v3
entry: wolfssl_3579_mul_rv32_bad_model
c_evidence:
- fixtures/wolfssl-3579-mul/target-bad/wolfssl_3579_mul_vulnerable.c
secret:
- arg: 0
  name: secret_a
- arg: 1
  name: secret_b
public:
- observable: control
- observable: timing
expect:
  final:
    model:
      status: Counterexample
      bad_state: target-control-successor-mismatch
    deployment: Open
    policy: Complete
    events:
    - kind: BranchSuccessor
      field: successor
      first_bad: true
    because: the hand-authored target helper model branches on secret_a while
      the real helper binding remains a separate deployment obligation
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - wolfssl_3579_mul_rv32_bad_model
        operation.names:
          ordered:
          - llvm.mlir.constant
          - llvm.icmp
          - llvm.cond_br
          - llvm.mul
          - llvm.return
        function.signatures:
          contains:
          - wolfssl_3579_mul_rv32_bad_model|(i64,i64)->i64
        branch.successor_shapes:
          contains:
          - wolfssl_3579_mul_rv32_bad_model|targets=bb1,bb2|same-target=false|args=;
    rv32-backend-risk:
      kind: assembly
      function: wolfssl_3579_mul_vulnerable
      properties:
        instruction.mnemonics:
          contains:
          - call
        call.callees:
          contains:
          - __muldi3

entry: wolfssl_3580_rv32_bad_model
observation-model: constant-time
principals:
- observer
adversaries:
  maximal:
  - - observer
hosts:
  compute:
    visibility: secret
components:
  table-input:
    lifecycle: entry-input
    type: bytes
    visibility: public
  table-index:
    lifecycle: entry-input
    type: bv32
    visibility: secret
outputs:
  return:
    visibility: secret
releases: {}

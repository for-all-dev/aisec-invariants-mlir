format_id: SPS-Harness-Fixture-Snapshot-v2
entry: wrong_host_fhe_reveal_fixed

c_evidence:
  - ../sources/wrong_host_fhe_reveal_fixed.c

secret:
  - {arg: 1, name: revealed_plaintext}

public:
  - {memory_at_arg: 3, name: unauthorized_server_plaintext}

allowed:
  - revealed plaintext may be written only to authorized_client_plaintext

expect: no-violation

because:
  - the unauthorized server receives zero while plaintext stays at the authorized client

sps: not-run

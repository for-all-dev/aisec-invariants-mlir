format_id: SPS-Harness-Fixture-Snapshot-v3
entry: wrong_party_plaintext_bad
c_evidence:
- fixtures/wrong-party-plaintext/bad/wrong_party_plaintext_bad.c
secret:
- arg: 0
  name: plaintext
public:
- memory_at_arg: 2
  name: unauthorized_mailbox
allowed:
- plaintext may be written only to authorized_mailbox
expect:
  final:
    model:
      status: Counterexample
      bad_state: unauthorized-mailbox-output-mismatch
    deployment: Open
    policy: Complete
    events:
    - kind: Output
      field: valueBytes
      first_bad: true
    because: plaintext is copied across the audience boundary
  pipelines:
    source-boundary:
      kind: diagnostic
      properties:
        completedChecks:
          contains:
          - ABICarriersResolved
          - CoalitionClosureEnumerated
          - NormalLLVMAnnotationResidueAbsent
          - PolicyIdsResolved
          - ReleaseLocatorResolved
          - SourceAnnotationsMatched
        findings:
          count:
            eq: 0
        blockers:
          count:
            eq: 0
    modeled-shape:
      kind: mlir
      properties:
        function.names:
          contains:
          - wrong_party_plaintext_bad
        operation.names:
          ordered:
          - llvm.store
        operation.occurrences.llvm.store:
          count:
            min: 2

entry: abi_alias_disjoint_control

secret:
  - {arg: 0, name: secret}

public:
  - {memory_at_arg: 3, name: public_output}

expect: relational-check

because:
  - secret is stored through p, but the public output reloads disjoint q

sps: not-run

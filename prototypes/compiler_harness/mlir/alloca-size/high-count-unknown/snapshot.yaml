entry: alloca_size_high_count

secret:
  - {arg: 0, name: secret_bit}

public:
  - {observable: allocation-size}

expect: unknown

because:
  - the secret selects allocation count 64 or 128, so the structural-size premise is unavailable

sps: not-run

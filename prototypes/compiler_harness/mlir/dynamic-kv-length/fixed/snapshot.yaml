entry: dynamic_kv_length_fixed

secret:
  - {arg: 0, name: secret_length}

public:
  - {memory_at_arg: 2, name: public_allocation_count}
  - {memory_at_arg: 3, name: public_iteration_count}

expect: no-violation

because:
  - both public count fields receive constant 64

sps: not-run

entry: launder_scan_model_proved

secret:
  - {arg: 0, name: secret}

public:
  - {observable: control}
  - {observable: address}
  - {observable: timing}

expect: target-risk

because:
  - a branchless LLVM select may become a secret branch and conditional load on x86

sps: not-run

entry: leftoverlocals_scratch_fixed

secret:
  - {arg: 0, name: prior_tenant_secret}

public:
  - {memory_at_arg: 3, name: next_tenant_output}

expect: no-violation

because:
  - scratch is overwritten with current tenant data before it is reloaded

sps: not-run

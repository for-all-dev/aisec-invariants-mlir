entry: overwritten_slot_control

secret:
  - {arg: 0, name: secret}

public:
  - {memory_at_arg: 3, name: public_sink}

expect: relational-check

because:
  - a public value fully overwrites the secret slot before the reload

sps: not-run

entry: secret_embedding_index_fixed

secret:
  - {arg: 1, name: secret_index}

public:
  - {observable: address}

expect: no-violation

because:
  - every run scans all 16 public addresses in the same order

sps: not-run

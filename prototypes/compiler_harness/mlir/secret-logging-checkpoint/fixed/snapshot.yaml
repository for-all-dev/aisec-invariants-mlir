entry: secret_logging_checkpoint_fixed

secret:
  - {arg: 0, name: service_account_token}

public:
  - {memory_at_arg: 2, name: public_log}
  - {memory_at_arg: 3, name: public_checkpoint}

expect: no-violation

because:
  - both public outputs receive zero while the token remains private

sps: not-run

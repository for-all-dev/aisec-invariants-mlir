entry: wolfssl_3579_mul_fixed_model

secret:
  - {arg: 0, name: secret_a}
  - {arg: 1, name: secret_b}

public:
  - {observable: control}
  - {observable: timing}

expect: no-violation

because:
  - a fixed 64-iteration mask-and-add loop avoids the helper and secret control

sps: not-run

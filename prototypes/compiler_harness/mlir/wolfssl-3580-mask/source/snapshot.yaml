entry: wolfssl_3580_select_source

secret:
  - {arg: 0, name: table_index}

public:
  - {observable: control}
  - {observable: timing}

expect: shape-only

because:
  - the source equality mask is branchless, while backend behavior is not represented

sps: not-run

entry: wrong_host_fhe_reveal_bad

secret:
  - {arg: 1, name: revealed_plaintext}

public:
  - {memory_at_arg: 3, name: unauthorized_server_plaintext}

allowed:
  - revealed plaintext may be written only to authorized_client_plaintext

expect: violation

because:
  - revealed plaintext is copied to a server not authorized for plaintext

sps: not-run
